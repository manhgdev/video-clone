/**
 * Clone Video workspace — currently rendered inside app/App.tsx
 * (ProjectSidebar + PipelineStepper + SegmentList).
 * Extract full page composition here in a later pass.
 */
export default function ClonePage() {
  return null
}
